language: cpp
compiler:
  - clang
  - gcc
install:
  - sudo apt-get install libgtest-dev
script:
  - cd /usr/src/gtest
  - ls -al
  - cmake CMakeLists.txt
  - make
  - cp *.a /usr/lib
  - cd -
  - cmake -G "Unix Makefiles" .
  - make
