language: cpp
compiler:
  - clang
  - gcc
install:
  - pwd
  - sudo apt-get install libgtest-dev
  - cd /usr/src/gtest
  - sudo cmake CMakeLists.txt
  - sudo make
  - sudo cp *.a /usr/lib
  - cd -
script:
  - cmake -G "Unix Makefiles" .
  - make
